package com.monitor.monitor.es.module;

public enum FilesetModule {
	traefik, system, logstash, postgresql, osquery, mongodb, nginx, kibana, kafka, iis, icinga, elasticsearch, redis,
	auditd, apache2,mysql
}
